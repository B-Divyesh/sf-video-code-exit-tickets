[
  {
    "id": "demo-pass",
    "claim": "Opens the sample without an account or setup, runs changed JavaScript, and passes when output is 6, 10, 14.",
    "where": "demo checkpoint",
    "test": "npm test -- --grep @claim:demo-pass",
    "sandbox": "fresh browser context at /demo using the bundled arrays lesson"
  },
  {
    "id": "privacy-demo",
    "claim": "Demo sample data is not saved and no demo request leaves the site origin.",
    "where": "demo banner and privacy page",
    "test": "npm test -- --grep @claim:privacy-demo",
    "sandbox": "fresh browser context; record requests and inspect local and session storage after passing"
  },
  {
    "id": "demo-exit-isolation",
    "claim": "Demo edits disappear on every exit, and the demo neither reads nor changes real extension data.",
    "where": "demo banner, privacy page, and demo documentation",
    "test": "npm test -- --grep @claim:demo-exit-isolation",
    "sandbox": "fresh browser and unpacked-extension contexts; pass the sample, leave through every advertised route and browser history, then assert starter state and unchanged seeded extension storage"
  },
  {
    "id": "timeout-recovery",
    "claim": "Stops an endless demo program within 1.5 seconds and lets the learner run corrected code.",
    "where": "demo checkpoint and README",
    "test": "npm test -- --grep @claim:timeout-recovery",
    "sandbox": "fresh browser context at /demo under production headers; measure click to rendered timeout against 1,500 ms with a 150 ms browser scheduling margin, then run the bundled corrected arrays program"
  },
  {
    "id": "extension-download",
    "claim": "Downloads the free packaged Chrome extension without an account or payment.",
    "where": "site header and README",
    "test": "npm test -- --grep @claim:extension-download",
    "sandbox": "fresh browser context; assert a run-before-next-chrome.zip download"
  },
  {
    "id": "manifest-export",
    "claim": "Existing Creator Kit users can verify a license and download a validated version 1 checkpoint file.",
    "where": "existing-license section and manifest builder",
    "test": "npm test -- --grep @claim:manifest-export",
    "sandbox": "fresh browser context with a recorded valid verification response; restore a license, assert the verification request, and inspect downloaded JSON fields"
  },
  {
    "id": "creator-sales-paused",
    "claim": "Creator Kit sales are paused and the unavailable checkout is not shown.",
    "where": "existing-license section, terms, and README",
    "test": "npm test -- --grep @claim:creator-sales-paused",
    "sandbox": "fresh browser context; assert the paused notice is visible and no checkout, purchase action, or price is advertised"
  },
  {
    "id": "sandbox-isolation",
    "claim": "Learner code runs in a declared browser sandbox whose outbound connections are blocked.",
    "where": "landing boundaries and README",
    "test": "npm test -- --grep @claim:sandbox-isolation",
    "sandbox": "built site and extension; assert app CSP excludes eval while both declared sandbox policies allow the runner and block connections"
  },
  {
    "id": "extension-flow",
    "claim": "Crossing a marked lesson time opens its checkpoint, pauses the video, records a passing run, and releases the lesson.",
    "where": "landing how-it-works section and README",
    "test": "npm test -- --grep @claim:extension-flow",
    "sandbox": "fresh Chromium profile with the unpacked built extension and the bundled lesson fixture"
  },
  {
    "id": "source-not-saved",
    "claim": "Learner source code is not uploaded or saved after a checkpoint pass.",
    "where": "landing facts, privacy page, and README",
    "test": "npm test -- --grep @claim:source-not-saved",
    "sandbox": "fresh Chromium extension profile; pass the fixture and inspect all chrome.storage.local values"
  },
  {
    "id": "template-allowlist",
    "claim": "Version one accepts only the JavaScript console sandbox template.",
    "where": "landing boundaries and author documentation",
    "test": "npm run test:unit -- --testNamePattern @claim:template-allowlist",
    "sandbox": "bundled valid manifest changed to an unknown remote-python template"
  },
  {
    "id": "checkpoint-sorting",
    "claim": "The extension sorts valid checkpoints by time.",
    "where": "README",
    "test": "npm run test:unit -- --testNamePattern @claim:checkpoint-sorting",
    "sandbox": "a valid two-checkpoint manifest supplied in reverse time order"
  },
  {
    "id": "video-pause-gate",
    "claim": "The lesson video stays paused until the checkpoint passes and the learner chooses Resume lesson.",
    "where": "landing, metadata, and README",
    "test": "npm test -- --grep @claim:video-pause-gate",
    "sandbox": "fresh Chromium extension profile with a playable MediaStream video; call video.play while the checkpoint dialog is active"
  },
  {
    "id": "video-local-only",
    "claim": "The extension checks the page video locally; it does not scrape, host, copy, or redistribute the video.",
    "where": "landing and README",
    "test": "npm test -- --grep @claim:video-local-only",
    "sandbox": "fresh Chromium extension profile with a local playable video; record all HTTP requests during checkpoint open, run, and resume"
  },
  {
    "id": "sandbox-no-extension-apis",
    "claim": "Learner code runs in a sandbox with no extension APIs.",
    "where": "README",
    "test": "npm test -- --grep @claim:sandbox-no-extension-apis",
    "sandbox": "fresh Chromium extension profile; run console.log(typeof chrome) in the declared extension sandbox"
  },
  {
    "id": "license-check-cadence",
    "claim": "License verification runs at most once daily and never blocks the free extension download.",
    "where": "README",
    "test": "npm test -- --grep @claim:license-check-cadence",
    "sandbox": "fresh browser with a stale recorded valid license; hold the verification response, confirm the download is usable, then reload after caching"
  },
  {
    "id": "site-local-assets",
    "claim": "The site loads its code and images from this domain and does not load analytics, ads, or third-party fonts.",
    "where": "privacy page",
    "test": "npm test -- --grep @claim:site-local-assets",
    "sandbox": "fresh browser visits every public route while recording requests"
  },
  {
    "id": "no-manifest-inert",
    "claim": "On a page without an author checkpoint file, the extension does not change storage, network activity, or video playback.",
    "where": "landing boundaries and privacy page",
    "test": "npm test -- --grep @claim:no-manifest-inert",
    "sandbox": "fresh Chromium extension profile with a playable same-origin video and no checkpoint manifest; inspect playback, requests, UI, and extension storage"
  },
  {
    "id": "sample-manifest-download",
    "claim": "Downloads a valid sample checkpoint file with the documented arrays check at 47 seconds.",
    "where": "landing author section",
    "test": "npm test -- --grep @claim:sample-manifest-download",
    "sandbox": "fresh browser context; download sample-manifest.json, parse it with validateManifest, and assert its documented fields"
  },
  {
    "id": "unique-checkpoint-ids",
    "claim": "Checkpoint IDs must be unique.",
    "where": "README and builder validation",
    "test": "npm run test:unit -- --testNamePattern @claim:unique-checkpoint-ids",
    "sandbox": "valid sample manifest with the same checkpoint ID added twice"
  },
  {
    "id": "offline-reload",
    "claim": "The demo reloads offline after one online visit.",
    "where": "README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load /demo online, activate the service worker, set the context offline, reload, and assert the sample checkpoint"
  },
  {
    "id": "manifest-round-trip",
    "claim": "Existing Creator Kit users can import, edit, reorder, add, remove, and export multi-checkpoint files locally.",
    "where": "landing existing-customer section, creator page, and README",
    "test": "npm test -- --grep @claim:manifest-round-trip",
    "sandbox": "fresh browser with a recorded valid license; import a two-checkpoint fixture, edit and reorder it, add and remove one checkpoint, then validate the downloaded JSON"
  },
  {
    "id": "manifest-import-recovery",
    "claim": "An invalid checkpoint file shows a field-linked error and can be replaced with a valid file.",
    "where": "creator page",
    "test": "npm test -- --grep @claim:manifest-import-recovery",
    "sandbox": "fresh licensed browser state; import malformed JSON, assert the announced input error, then import and export a valid two-checkpoint file"
  }
]
